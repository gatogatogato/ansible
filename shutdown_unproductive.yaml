---
#- hosts: flickrservers,testservers,ansibleservers
- hosts: test1,test2,hercules,flickr,ansible,rclone
  gather_facts: true
  become: true
  name: "Shutdown nonproductive servers"
  tasks:
    - name: "Execute shutdown in 30s"
      community.general.shutdown:
        delay: 30
